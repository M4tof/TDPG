namespace TDPG.EffectSystem.ElementLogic
{
    // PUT ALL POSSIBLE PARAMS HERE, NO UNDEFINED LOGIC CAN BE ACCEPTED
    public enum EffectParameter //until there is only 64, we can use one seed per element
    {
        Duration,
        HealthChange,
        SlowdownFactor,
    }
    
}